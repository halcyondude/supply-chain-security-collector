{
  "name": "cncf-supply-chain-explorer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "export-db": "cd .. && npx ts-node --esm site/scripts/export-base-db.ts"
  },
  "dependencies": {
    "preact": "^10.25.0",
    "@preact/signals": "^1.3.0"
  },
  "devDependencies": {
    "vite": "^6.3.0",
    "@preact/preset-vite": "^2.10.0",
    "typescript": "^5.7.0"
  }
}
